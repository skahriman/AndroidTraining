<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sefakkahriman.homework">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".problem3.MyCustomApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".problem1.MyTimberActivity" />
        <activity android:name=".problem2.BarcodeReaderActivity" />
        <activity android:name=".problem3.LeakCanaryActivity" />
        <activity android:name=".problem4.FurryAnalyticsActivity"></activity>
    </application>

</manifest>